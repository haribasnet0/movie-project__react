import React from 'react';
import './LoadMoreBtn.css';

const LoadMoreBtn = (props) => {
    return (
        <div>
            loadmorebtbb is also awesome 
        </div>
    )
}

export default LoadMoreBtn;